export default defineAppConfig({

  ui: {
    input: {
      slots: {
        base: 'w-full',
      },
    },
    select: {
      slots: {
        base: 'w-full',
      },
    },
    selectMenu: {
      slots: {
        base: 'w-full',
      },
    },
  },

});
